import 'package:flutter/material.dart';

class ManageToken extends StatelessWidget {
  const ManageToken({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Token Page"),
      ),
    );
  }
}